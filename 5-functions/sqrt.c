#include<stdio.h>
#include<math.h>
int main(){
    int a;
    printf("Enter a number : ");
    scanf("%d",&a);

    // sqrt meaning => square root 
    int root = sqrt(a); 
    printf("The square root is : %d",root);
   
    // pow meaning => power
    // int q = pow(2,6); // number change and the final answer
    // printf("%d",q);
    //  return 0;
}