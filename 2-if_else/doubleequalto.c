// if we have only one print statment with if condition then no need to put curly bracket

#include <stdio.h>
int main()
{
    int n = 2;
    if (n == 2)
        printf("good moring");
    else
        printf("good evening");
    

    return 0;
}